/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["localhost:8080", "links.papareact.com", "getbootstrap.com", "res.cloudinary.com"]
    }
}

module.exports = nextConfig
